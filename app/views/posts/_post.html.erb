<li>
  <%= post.status %> -<%= post.name %>
  <%= link_to image_tag("social-media-like-button.jpg", size: "32"), like_path(post), method: :post, :remote => true %>
  <span id="like-<%=post.id%>"><%= post.likes %></span>
</li>
